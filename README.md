# Creazione prompt

voglio creare una TO-DO list usando slamente html,css e js.
l'utente può inserire nuovi dati ed eliminarli, no modifica no ricerca.
usa supabase come gestore dei dati.
aggiungi l'autenticazione tramite supabase per gli utenti.
devi creare un file separato dal nome "DB.sql" in cui ci sono tutte le querry da eseuire su supabase.
in questo file devono esserci le istruzioni per le create table e le rls policy.
aggiungi anche il realtime, tramite chanel.
Nel codice come variante del client usa "supabaseClient"
